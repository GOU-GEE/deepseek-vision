name: publish

on:
  release:
    types: [published]

jobs:
  pypi:
    runs-on: ubuntu-latest
    permissions:
      id-token: write

    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Build distributions
        run: |
          python -m pip install --upgrade build twine
          python -m build
          python -m twine check dist/*

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1

  npm-dsh-plugin:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write

    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v6
        with:
          node-version: "22.19.0"
          registry-url: https://registry.npmjs.org

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install build dependencies
        run: |
          npm ci --ignore-scripts --prefix plugins/dsh-plugin-deepseek-vision
          python -m pip install --upgrade build

      - name: Test and publish DSH bundle
        working-directory: plugins/dsh-plugin-deepseek-vision
        env:
          VISION_BUILD_PYTHON: python
          # 使用 npm 账号的发布 token（包所有者身份），而非 OIDC 派生身份。
          # OIDC 身份无法向已存在的包名发布（404），token 身份则可以。
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          npm test
          npm publish --access public --provenance
